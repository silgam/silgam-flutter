import 'package:flutter/material.dart';

class CustomExamEditPage extends StatelessWidget {
  static const routeName = '/custom_exam/edit';

  const CustomExamEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
