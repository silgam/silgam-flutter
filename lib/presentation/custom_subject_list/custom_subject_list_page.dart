import 'package:flutter/material.dart';

class CustomSubjectListPage extends StatelessWidget {
  static const routeName = '/custom_subject';

  const CustomSubjectListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
