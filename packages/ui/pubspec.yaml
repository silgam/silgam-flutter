name: ui
version: 0.0.1
publish_to: none

environment:
  sdk: 3.6.1
  flutter: 3.27.3

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_form_builder: ^9.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
